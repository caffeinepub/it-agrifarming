{
  "frontend/src/index.css": "Global styles with nature-inspired green OKLCH palette; adds new cute tree animations — `tree-wiggle`, `tree-bounce`, `sparkle-twinkle`, `heart-float`, `glow-pulse` keyframes with corresponding utility classes, plus `tree-glow` and `tree-glow-lg` CSS filter helpers for a soft green drop-shadow glow effect.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Fredoka/Nunito fonts, new nature color tokens (green, sage, mint, earth, forest), updated green-themed box shadows, and preserved accordion animations and plugins.",
  "frontend/src/hooks/useQueries.ts": "React Query mutation hook wrapping the backend `assessPlant` call, accepting `{ imageBytes: Uint8Array, name?: string }`, converting to `ExternalBlob.fromBytes()` with an explicit `ArrayBuffer` cast, and returning a full `Plant` result with the `needs` array.",
  "frontend/src/components/LoadingSpinner.tsx": "Animated loading component with spinning leaf/sprout emojis; the center 🌳 tree is now larger with a bouncy `tree-bounce` animation, a `tree-glow-lg` drop-shadow, and four surrounding accent emojis (✨💚🌸💛) that twinkle and float for a cute, lively feel.",
  "frontend/src/components/PlantResults.tsx": "Results display component that shows a prominent emotion card at the top (with a large animated emoji, bold child-friendly label, and age-appropriate message for all five emotions: happy 😊, sad 😢, angry 😠, worried 😟, upset 😤), followed by the overall health summary and five individual NeedCard components for sunlight, water, soil, air quality, and pest detection — all with black text in the nature-green theme.",
  "frontend/src/pages/PlantScanner.tsx": "Main scanner page with upload/camera/results view states; stores the full `Plant` result (instead of `HealthStatus`) and passes it to `PlantResults`, with the `CuteMascotTree` upload zone mascot and animated analyze button unchanged.",
  "frontend/src/components/Layout.tsx": "Full-page layout wrapper with botanical background and footer; the header now uses a reusable `CuteTree` component that wraps the 🌳 emoji with a gentle wiggle animation, green glow, and flanking ✨/💚 sparkle/heart accents that twinkle and float.",
  "frontend/src/App.tsx": "Root app component that wraps PlantScanner inside the Layout component for consistent theming.",
  "frontend/src/components/QRCodeCanvas.tsx": "Self-contained QR code generator component that implements the full QR code algorithm (Reed-Solomon ECC, masking, format/version info) in pure TypeScript and renders it onto an HTML5 canvas element with configurable size and colors."
}